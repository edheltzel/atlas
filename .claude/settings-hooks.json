{
  "_description": "Hook registration template for Claude Code settings.json",
  "_instructions": [
    "Merge these hook configurations into ~/.claude/settings.json",
    "The hooks array should be added/merged under the 'hooks' key",
    "Adjust paths based on your $PAI_DIR location"
  ],
  "_performance_notes": [
    "Phase 5 finding: Bun's interpreted execution is already ~25ms",
    "Compiled binaries provide no significant improvement",
    "Recommend: Keep using interpreted TypeScript for easier development",
    "Build script available at $PAI_DIR/hooks/build.sh if binaries needed"
  ],
  "hooks": [
    {
      "event": "Stop",
      "command": "bun run $PAI_DIR/hooks/stop-hook-voice.ts",
      "description": "Main agent voice notification with prosody enhancement"
    },
    {
      "event": "SubagentStop",
      "command": "bun run $PAI_DIR/hooks/subagent-stop-hook-voice.ts",
      "description": "Subagent voice notification with personality-specific delivery"
    }
  ],
  "_compiled_hooks_alternative": {
    "_note": "Optional: Use compiled binaries (run ./hooks/build.sh first)",
    "hooks": [
      {
        "event": "Stop",
        "command": "$PAI_DIR/hooks/bin/stop-hook-voice"
      },
      {
        "event": "SubagentStop",
        "command": "$PAI_DIR/hooks/bin/subagent-stop-hook-voice"
      }
    ]
  },
  "_example_settings_json": {
    "hooks": [
      {
        "event": "Stop",
        "command": "bun run /Users/yourname/.config/pai/hooks/stop-hook-voice.ts"
      },
      {
        "event": "SubagentStop",
        "command": "bun run /Users/yourname/.config/pai/hooks/subagent-stop-hook-voice.ts"
      }
    ]
  }
}
